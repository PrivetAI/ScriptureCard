workflows:
  ios-native-quick-start:
    name: iOS Native
    max_build_duration: 60
    instance_type: mac_mini_m2
    integrations:
      app_store_connect: 'Scripture Card'
    environment:
      # Подпись берётся из хранилища Codemagic (Settings → Code signing
      # identities): там лежит distribution-сертификат «Scripture Card» вместе
      # с приватным ключом и подтянутые из портала provisioning profiles на
      # com.alfonsocochran.scripturecard и на виджет .widget. По документации
      # bundle_identifier подхватывает и профили расширений, отдельно их
      # перечислять не нужно. Один сертификат обслуживает все сборки.
      #
      # Прежняя схема генерировала ключ заново на каждой сборке
      # (openssl genrsa) — сертификат оставался, а ключа к нему больше не
      # было, поэтому каждый запуск просил у Apple новый и упирался в лимит:
      # «You already have a current Distribution certificate», 409.
      ios_signing:
        distribution_type: app_store
        bundle_identifier: com.alfonsocochran.scripturecard
      vars:
        BUNDLE_ID: "com.alfonsocochran.scripturecard"
        XCODE_WORKSPACE: "Scripture Card.xcodeproj"
        XCODE_SCHEME: "Scripture Card"
        APP_STORE_APPLE_ID: 6804661083
      xcode: latest
    scripts:
      - name: Set up provisioning profiles settings on Xcode project
        script: xcode-project use-profiles
      - name: Build ipa for distribution
        script: |
          xcode-project build-ipa \
            --project "$CM_BUILD_DIR/$XCODE_WORKSPACE" \
            --scheme "$XCODE_SCHEME"
    artifacts:
      - build/ios/ipa/*.ipa
      - /tmp/xcodebuild_logs/*.log
    publishing:
      app_store_connect:
        auth: integration
        submit_to_testflight: false
